#conda env list (sanal ortamları sıralama)
#conda create -n isim (isim adında sanal ortam oluşturur)

#conda activate isim (isim adındaki sanal ortama girme)
#conda deactivate (sanal ortamdan çıkış)

#conda list (yüklü paketlerin listelenmesi)
#conda install (paket yükleme)
#conda remove (paket kaldırma)
#conda install numpy=1.20.1 (paketin belirli bir sürümünü yükleme (conda için))
#conda upgrade (paket güncelleme)

#pip install nunmpy==1.20.1(pip için sürüm yükleme)

#conda env export > environment.yaml(yml dosyası olarak paketlerin sürüm bilgilerini yml dosyasına yazıp kaydeder)
#conda env create -f environment.yaml(yeni ortama dosyadaki sürümleri aktararak oluşturma)

